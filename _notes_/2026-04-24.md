Claude Code Rules:
* prefer project based .claude folder
* you own a .project .claude/CHANGES.md log of any changes with timestamps, duration of plan/execution, and token usage for each session.   always update it before you say you're done
* always use plan mode before doing anything, save the plan to ***project*** **.claude/plans/**
* move the plan to the **project** .**claude/plans** folder, prefix it with YYYYMMDDHHMM (i.e 20260424_2100_initial-commit-tag-push)
*

git checkout -b develop && git push -u origin develop
git checkout -b feature/migrate-legacy
